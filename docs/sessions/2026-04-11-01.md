# Session 2026-04-11-01: Multi-tenant provisioning end-to-end

## What was done

Single long session that turned a working single-tenant deployment into a functional multi-tenant platform. Six stages of work, all merged and deployed to `api.leofarmacia.com` + `app.leofarmacia.com`.

### Stage-by-stage

**Phase A — Multi-tenant backend foundation** (commits `257ab7f`, `46e7d9d`)

1. `seedDefaultStore()` — adopts the existing `odoo` DB as `store_leo` / Farmacia Leo on API startup. Idempotent. First tenant keeps all 110 existing products.
2. `DELETE /api/v1/admin/pharmacies/:id?confirm=yes` — reverse-order cleanup (Meilisearch → Odoo DB → Mongo records). Refuses default store. Requires explicit confirm query param.
3. Random per-pharmacy admin passwords — 24-char base64url, bcrypt hash on Store doc, plaintext carried through job step data only to the steps that need it and the email step for later retrieval.
4. Partial-failure verification — `odoo_db_create` step authenticates as the expected admin to verify the DB is actually healthy (not a half-created corpse from a previous crash). Drops and recreates if unhealthy. Post-create verification fails loud if master password is wrong.
5. **`request.store` middleware + scoped Odoo routing** — the big one. New `store-context.plugin.ts`, `odoo-scoped-cache.ts`, `odoo-store-ops.ts`. Refactored `orders`, `products`, `stats` to use `request.odoo` instead of the global singleton. Added `request.store` + `request.odoo` types to Fastify.
6. Super-admin UI at `/admin/pharmacies` — list table with status badges, auto-refresh every 3s while provisioning, create modal, details drawer with step-by-step job progress, retry + delete buttons.

**Phase A.5 — Closing the onboarding gap** (commit `6ac8d96`)

1. New 7th step `create_dashboard_admin` — creates a pharmacist-role `Admin` doc in Mongo with `owner_email` as login and the same random password as Odoo. Idempotent: appends store to existing admin if email already exists.
2. Credentials visible in admin UI — `email_credentials` step now retains plaintext password in step data until super-admin clicks "Marcar como entregadas". New endpoint `POST /admin/pharmacies/:id/credentials/mark-delivered` scrubs. Details drawer shows an amber panel with copy-to-clipboard for URL/email/password.
3. Super-admin store switcher — `Shell.tsx` fetches `/admin/pharmacies` for role=admin users and feeds active pharmacies into the existing header dropdown. Lets super-admin view any tenant's data.

**Phase B1–B5 — Make the WhatsApp→Odoo loop multi-tenant** (commit `404fc44`)

1. **B1**: `catalog-sync.service` iterates all active stores on each tick. Replaced hardcoded `['store_leo']`. Each new pharmacy gets its Meilisearch index populated automatically.
2. **B2**: `CommandContext` gains `store` + `odoo` fields. `commands.routes` resolves the store and attaches a scoped client before dispatching. `pedido.handler` rewritten to use `ctx.odoo` for every RPC.
3. **B3**: New `store-resolver.ts` — resolves Store by `whatsapp_instance_id` with a 60s in-process cache. Webhook now drops messages from unknown instances instead of silently misrouting them.
4. **B4**: Webhook forwards `store_config { name, currency, timezone, lang, agent: { name, greeting_style, signature, business_hours, delivery_info, custom_notes } }` to n8n alongside the message text. n8n agents read this to personalize replies.
5. **B5**: New `/agent` "Mi Agente" page in the dashboard with form + live greeting preview. New `GET /api/v1/stores/:storeId` and `PATCH /api/v1/stores/:storeId/agent-config` endpoints (scoped, length-validated, cache-invalidating).

**Phase B6 — WhatsApp multi-connection + Evolution integration** (commits `9c7088f`, `c75ed39`, `8b8af7a`, `776b843`, `e5f0749`, `d3ba595`)

1. **WhatsApp multi-connection model**: New `WhatsappConnection` Mongo model and `connection.service.ts`. Each store can have multiple Evolution connections (active/connecting/disconnected), each tracked with `instanceName`, `instanceId`, `status`, `label`, and `qrCode`. Dashboard `/whatsapp` page drives connect/disconnect flows.
2. **Evolution integration hardened**: `evolution.client.ts` updated with `createInstance`, `connectInstance`, `disconnectInstance`, `deleteInstance`, and `instanceInfo` methods. Connection flow creates an Evolution instance, stores the connection doc, and polls for QR code / connection status.
3. **Reply sending implemented**: Webhook handler now POSTs replies back to Evolution API via `sendText` after n8n responds. Uses the store's connection `instanceName` to route the reply to the correct WhatsApp number.
4. **Simplified disconnect**: Disconnect flow deletes the Evolution instance and marks the `WhatsappConnection` doc as `disconnected`. No complex state machine — clean create/delete lifecycle.
5. **Debug logging for webhook pipeline**: Added extensive `console.log` tracing through the entire webhook handler path (message receipt → store resolution → debounce → n8n forward → reply send). This was added to diagnose a silent message drop bug that is still open.
6. **End-to-end testing session**: Provisioned `farmacia_geremy` via the UI (7-step pipeline works). Tested pharmacist login (found JWT `stores` field bug — was array of ObjectIds instead of store_id strings — fixed). Connected WhatsApp via dashboard QR flow. Sent test messages — messages arrive at the API webhook but are silently dropped before reaching n8n. Bug is still open, debug logging is in place for next session.

### ADRs + docs written

- **ADR-007 — Tiered POS sync-back strategy** (`docs/decisions/007-tiered-pos-sync.md`): <5 units human-in-the-loop, 5-10 real-time push, >10 nightly reconciliation. Solves concurrent overselling naturally at the critical tier via serialized pharmacist confirmations.
- **Risk #11 in `known-risks.md`** — walk-in oversell while WhatsApp sells low-stock items.
- **Stage 6 (POS Sync)** updated with the bidirectional flow, expanded `PosAdapter` interface, Option A/B onboarding modes.
- **Stage 8 (new) — Multi-tenant Provisioning** — full architecture doc for the work done this session.
- **`testing-pending.md`** — explicit list of untested flows, sorted by criticality, with a suggested single smoke-test path that covers 80% of the red rows.

### Infrastructure changes

- **Odoo master password** set via a shell entrypoint wrapper in `docker-compose.yml` that writes `admin_passwd = ${ODOO_ADMIN_PASSWD}` into `/etc/odoo/odoo.conf` before exec-ing Odoo. Odoo 17 has no `--admin-passwd` CLI flag — required this approach.
- **`pos` service migrated from raw-compose-in-Dokploy to GitHub-backed** so the compose file in the repo is actually the source of truth. Previously, editing the repo had no effect on `pos`.
- **Dashboard domain** was accidentally set to `localhost` in the Dokploy Domain tab after some earlier edit — fixed to `app.leofarmacia.com`, traffic restored.
- **Memory saved**: repo is source of truth, secrets in Dokploy env, never hardcode on VPS. `pos` exception noted (raw → github migration done this session).

## Verified end-to-end

1. **Provisioning**: created `farmacia_test` via curl, watched 6 steps advance through the job, ~25s total. Verified Odoo DB was created and healthy by authenticating.
2. **DELETE endpoint**: tested all three safeguards (missing confirm → 400, targeting `store_leo` → refused, valid delete → reverse cleanup).
3. **Scoped routing**: `/stores/store_leo/products` returns the 110 Farmacia Leo products, `/stores/store_ghost/products` returns 404.
4. **seedDefaultStore**: Farmacia Leo auto-adopted the existing Odoo DB on startup, visible in admin UI.
5. **Dashboard loads** at `app.leofarmacia.com` after the domain fix.
6. **7-step provisioning via UI**: created `farmacia_geremy` from the admin dashboard. All 7 steps completed successfully including `create_dashboard_admin`.
7. **Pharmacist login**: logged in with provisioned credentials for `farmacia_geremy`. Initially got 403 — JWT bug found (`stores` field contained ObjectIds instead of `store_id` strings). Fixed and login works.
8. **WhatsApp connection via dashboard**: connected a WhatsApp number to `farmacia_geremy` through the QR flow in `/whatsapp` page. Connection tracked in `WhatsappConnection` collection.
9. **n8n receiving payloads**: tested via direct `curl` to the n8n webhook URL — n8n processes and responds correctly.
10. **n8n responding**: confirmed n8n returns valid reply text when given a well-formed payload.

## Still broken / untested

See `docs/status/testing-pending.md`. Key items after Phase B6:
- **WhatsApp webhook → n8n flow**: messages arrive at the API webhook endpoint but are silently dropped before reaching n8n. Debug logging added, diagnosis pending for next session.
- **Evolution reply sending**: code written but untested end-to-end (blocked by the webhook silent drop bug above).
- **Catalog sync for new pharmacies**: `farmacia_geremy` catalog sync fails because scoped Odoo auth uses `config.odoo.user` (admin) instead of the store-specific owner credentials or the master admin.

## Known blockers before first real customer

1. **Webhook silent message drop** — WhatsApp messages arrive at the API but never reach n8n. Debug logging added in Phase B6, diagnosis pending. This is the #1 blocker.
2. **Catalog sync auth bug** — scoped Odoo auth for new pharmacies uses `config.odoo.user` (the global admin email) instead of store-specific credentials or master admin. Causes catalog sync to fail for `farmacia_geremy`.
3. **Evolution reply sending** — code written in Phase B6 but untested (blocked by #1).
4. **n8n agents** — user-owned, in progress.
5. **POS connector for first pharmacy** — blocked on first customer decision.

## Next session

Proposed work, in priority order:

1. Diagnose and fix the webhook silent message drop (debug logging is already in place)
2. Fix catalog sync auth bug (use master admin or per-store credentials for Odoo scoped client)
3. Once #1 is fixed, test the full WhatsApp → n8n → reply loop end-to-end
4. Run the smoke test from `testing-pending.md` and fix whatever falls over

## Commits (in order)

- `1f7f966` — Harden Odoo: require ODOO_ADMIN_PASSWD env + hide db manager
- `83b1468` — Add pharmacy provisioning: Store model + state machine + super-admin API
- `f66d392` — Fix Odoo master password: write to odoo.conf (no CLI flag exists)
- `257ab7f` — Phase A: multi-tenant backend — scoped routing, default store, DELETE, hardening
- `46e7d9d` — Phase A step 6: super-admin /admin/pharmacies UI
- `6ac8d96` — Phase A.5: dashboard admin auto-provisioning + credentials UI + super-admin switcher
- `404fc44` — Phase B1-B5: finish multi-tenant wiring for WhatsApp→Odoo loop
- `9c7088f` — WhatsApp multi-connection model + connection service
- `c75ed39` — Evolution integration: create/connect/disconnect instance flows
- `8b8af7a` — WhatsApp dashboard page: QR connect + disconnect UI
- `776b843` — Reply sending via Evolution API + webhook debug logging
- `e5f0749` — Fix JWT stores field bug (ObjectIds → store_id strings)
- `d3ba595` — Catalog sync auth fix attempt + additional debug logging
