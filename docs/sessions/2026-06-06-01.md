# Session 2026-06-06-01: Mapa de negocio, Stage 10 y M1 verificado en producción

Tres arcos: (1) análisis de valor + mapa arquitectónico del modelo de negocio,
(2) diseño del MVP escalable (Stage 10) a partir del flujo probado de NeoColmado,
(3) implementación y verificación e2e en producción del milestone M1.

## Arc 1 — Valor y mapa de negocio

- Análisis de valor para farmacias (dolor, mapa dolor→feature, modelo económico
  ~RD$60-90k/mes de valor por farmacia, jerarquía: WhatsApp núcleo → voz
  diferenciador → refill lock-in futuro).
- **`ARQUITECTURA_MODELO_NEGOCIO.md`** (raíz): espejo del documento de auditoría
  de NeoColmado con indicadores ✅/⚠️/⏳/💡 por capacidad/flujo/componente.
  Captura el modelo de inventario **inverso** al colmado (catálogo completo desde
  el día 1; conector POS como estándar futuro, ADR-007) y la lección RTDB
  aplicada (contratos solo vía API Fastify).

## Arc 2 — Stage 10: MVP escalable

`docs/stages/10-mvp-escalable.md` — 4 milestones: M1 alta sistemática, M2 patrón
✗ de despacho, M3 voz en el ciclo (Phase F+G), M4 health board + e2e formal.
Decisiones: D1 usuario de servicio compartido, D2 seed por copia JSON-RPC,
D3 disponibilidad por patrón ✗ (difiere ADR-007), D4 aviso de ✗ vía n8n con
contexto.

## Arc 3 — M1 implementado y verificado en producción

### Hallazgo: el bug de auth era peor de lo documentado
`getScopedOdoo(config, db)` con credenciales globales se usa en catalog-sync,
**`/api/v1/commands` (pedidos) y store-context (todas las rutas del dashboard)**
— una farmacia nueva no solo tenía índice vacío: tenía TODO el acceso a su Odoo
roto.

### Commits
- `dafdfc2` — docs: mapa de negocio + spec Stage 10 + roadmap
- `e935216` — Fix auth: `odoo_seed_admin` crea usuario de servicio interno
  (login/password del env, grupos del admin) en cada DB nueva. `ensureServiceUser`
  idempotente en `odoo-store-ops.ts`. Ruta de reparación
  `POST /admin/pharmacies/:storeId/repair-odoo-service`. `ScopedOdoo.execute`
  gana timeout opcional.
- `757bdbc` — Paso **`odoo_seed_catalog`** (pipeline pasa de 7 a 8 pasos):
  instala módulos en la DB recién creada (nace solo con `base` — ¡sin
  `product.product` siquiera!), copia categorías + productos del catálogo
  maestro (`MASTER_CATALOG_DB`, default la DB principal) por lotes, idempotente,
  barcodes duplicados degradan a `false` en vez de romper el batch.
  `meilisearch_index` ahora dispara `fullRebuildStoreIndex` inmediato.
- `8897aad` — **Fix descubierto en producción**: `sale_management` NO instala
  `stock`, y `qty_available` (leído por catalog-sync, búsqueda de productos y
  `pedido.consultarPrecio`) es un campo de `stock`. Primera corrida falló en
  `meilisearch_index` con `Invalid field 'qty_available'`. `ensureModules`
  instala ambos.
- `76c9fa7` — Silenciado el spam de deprecación de Mongoose (`new: true` →
  `returnDocument: 'after'`) que inundaba los logs cada tick del worker (5s).
  Nota: un intento de hacer este replace con PowerShell dañó el encoding del
  archivo (mojibake + BOM) — se revirtió y rehízo con la herramienta de edición.
- `92dffa9` — **Tercer fix de la misma clase**: las rutas de productos leen
  `use_expiration_date`, que solo existe con `product_expiry` instalado →
  `/products` vacío en farmacias nuevas. `REQUIRED_MODULES` final:
  `sale_management`, `stock`, `product_expiry` (este último es además dominio
  core de farmacia: lotes/vencimiento).

### Verificación e2e (producción, 2026-06-06 ~5-6 p.m.)
1. Primera farmacia de prueba: falló en `meilisearch_index` (lo del módulo
   `stock`) — los primeros 5 pasos OK, incluido el seed (24.2s).
2. Tras `8897aad`: **`farmacia_test_m2` provisionada con los 8 pasos en verde.**
3. Evidencia en logs:
   - Catálogo maestro: **165 productos** (store_leo, rebuild `count:165`)
   - `farmacia_test_m2`: **165 productos** en su índice — copia 1:1 + 91 sinónimos
   - El rebuild autenticó contra `pharmacy_farmacia_test_m2` con el usuario de
     servicio (el camino que antes fallaba)
   - Sync periódico corre ambas farmacias sin errores
4. `farmacia_geremy` (rota, pre-fix) **eliminada** — su job viejo no tenía el
   paso de catálogo y su password ya no estaba disponible; re-provisionar era
   la vía correcta, no reparar.
5. Sonda de versión sin endpoint de health: ruta nueva 404→401 = deploy vivo.
6. Tercera corrida (post `92dffa9`): pipeline verde de nuevo y **`/products`
   mostrando el catálogo en el dashboard** — M1 verificado de punta a punta,
   incluida la vista del farmacéutico.

### Hallazgo: el catálogo real estaba en Meilisearch hace tiempo
Inspección de la instancia compartida de Meilisearch (melisearch.onrpa.com,
solo lecturas — la comparten los colmados):

- **`pharmacy_inventory`: 17,456 productos REALES** (export de POS de farmacia
  real: `codigo/descripcion/precio/unidad`, precios en DOP). El usuario
  confirma: ese es el catálogo que se debe usar. El maestro actual (165, los
  CAROL del Odoo de Leo) era un subset de prueba.
- Índices farmacia correctos: `store_store_leo_products` (165) y
  `store_farmacia_test_m2_products` (165, copia 1:1 ✓).
- `store_leo_products` (165): huérfano con naming viejo — limpieza pendiente
  con cuidado (instancia compartida).
- Índices colmado (`colmado_inventory` + 12 `productos_colmado_*`): intocables.

**Plan M1.5 (siguiente bloque)**: importador `pharmacy_inventory` → DB Odoo
dedicada `pharmacy_master_catalog` (NO el POS vivo de Leo) → setear
`MASTER_CATALOG_DB` en Dokploy env → toda farmacia nueva nace con los 17,456.
Pendiente confirmar vigencia de los precios del export.

### Gap de observabilidad detectado (anotado, no corregido)
`upsertDocuments` postea a Meilisearch y loguea "enviados" — pero Meili indexa
asíncrono vía tasks y nadie verifica el resultado de la task. Si una task
falla, el log dice éxito. Añadir verificación de task (o al menos log del
taskUid) — candidato para M4/health board.

### Pendiente / siguiente sesión
1. **M1.5 — catálogo real**: importador pharmacy_inventory → pharmacy_master_catalog.
2. **M2 — patrón ✗**: acciones ✓/✗ por ítem en `/orders`, evento → n8n → aviso
   IA al cliente, despacho con notificación.
3. M3 voz (Phase F+G mínimo), M4 health board + e2e formal con n8n.
4. Deuda visible: no hay endpoint `/health` (la sonda 404/401 fue workaround);
   `docker-compose.dokploy.yml` sigue sin commitear con JWT placeholder;
   prueba a nivel comandos va en M4; verificación de tasks de Meilisearch;
   huérfano `store_leo_products` por limpiar.
